const userController = require("./user.controller");
const authController = require("./auth.controller");
const postController = require("./post.controller");

module.exports = { userController, authController, postController };
